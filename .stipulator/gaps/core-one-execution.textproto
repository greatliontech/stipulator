# proto-file: proto/stipulator/v1/records.proto
# proto-message: stipulator.v1.Gap

requirement_id: "REQ-core-one-execution"
reason: "the invariant holds by construction — check executes the accepted policy once, and standalone verify, gate, and prune consume the policy's witness-only selective execution with no independent suite beside it — but the invariant cell demands property-class evidence and every bound witness is an example anchor"
lands { manual { condition: "a property witness or analyzer proof quantifies over policy execution: no reachable operation can execute test work outside the accepted policy" } }
