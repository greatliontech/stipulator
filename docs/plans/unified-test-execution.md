# Unified test execution

- [x] 1. Triage the execution-related issues and amend the overview, evidence, Go-backend, MCP, report-wire, and gate contracts so one explicit accepted test policy produces complete suite health and current-run witness evidence; conserve package build and exit failures, `init`, `TestMain`, executable examples, committed fuzz-seed replay, packages without named tests, and every workspace member; bind outcomes and observations to their exact producing invocation and process; state that per-test freshness can grant witness evidence but never package, command, or suite health; permit non-race invocations to contribute suite health but never Go witness evidence; declare progress, cancellation, and unified check result semantics; create validated gaps for every newly unsatisfied requirement with landing conditions mapped to later chunks; fold the discarded-suite, explicit-policy, isolated-gate-diagnostic, and long-running MCP issues, fold process-tree ownership for resolution in chunk 4, and explicitly redefer the runtime-input read-to-hash race to atomic value observation rather than implying this plan closes it.
- [x] 2. Define and generate the protobuf policy and report messages before any runner depends on them: backend-neutral invocation envelopes with typed backend payloads, canonical invocation identity, suite-health dispositions, completeness omissions, progress events, failure diagnostics, and the unified check result; define the textproto policy location and canonical ordering; reserve wire names and numbers; fuzz ProtoJSON and textproto malformed/unknown/duplicate/null cases; and publish deterministic binary and JSON round-trip fixtures for CLI and MCP consumers.
- [x] 3. Implement backend-neutral policy loading, validation, and migration without teaching core verification Go semantics: dispatch typed policy payloads to the named backend, reject unsupported backends and path escape, and expose only canonical invocation and health facts to orchestration; add one idempotent `policy init` migration that derives the current universal-race workspace policy, writes the separate policy textproto only when absent, refuses divergent overwrite, reports the configuration break explicitly, and has golden fixtures proving two hosts derive byte-identical output from the same workspace declaration.
- [x] 4. Implement Go policy normalization and complete workspace discovery through owned, cancellable process boundaries: pin the effective toolchain selection, normalized environment inheritance and denial, `GOOS`, `GOARCH`, CGO, tags and `GOFLAGS`, workspace mode, module/download behavior, PGO/build inputs, test count/cache mode, timeout, arguments, module root, and package scope; reject unsupported ambient controls; enumerate named tests, examples, committed fuzz seeds, external tests, and packages with no runnable test; report every omitted or multiply selected obligation; property-test partition conservation across workspaces and build selections; and close the package-loading process-tree issue only after descendant termination is proven on every supported platform, otherwise redefer it with the unsupported facility named.
- [x] 5. Execute each normalized Go policy invocation once under caller cancellation and bounded package concurrency, preserving ordinary package build, `init`, `TestMain`, example, fuzz-seed, timeout, race, cache/count, and exit behavior; give every selected package and invocation a terminal health disposition, retain bounded failure output, refuse silent or malformed command streams, discard partial reports on cancellation, and regression-test build-only packages, no-test packages, red `TestMain`, package panic, timeout, executable examples, fuzz seeds, and workspace-member failures without yet promoting any binding evidence.
- [x] 6. Add per-process observation and deterministic event attribution to the policy executor: collect independent completed or incomplete testlogs, named top-level and subtest outcomes, runtime registrations, process/package failures, and observation ownership without cross-process merging; fuzz malformed, truncated, reordered, duplicate, and post-abort event/testlog streams; prove that bytes from incomplete children never become completed evidence; keep the runtime-input read-to-hash race explicitly redeferred unless a Gofresh change in this chunk can atomically bind observed values to the reads that produced the outcome.
- [x] 7. Derive suite health and verification witnesses from the same in-memory execution report: require a healthy producing package invocation before a passing child grants evidence, cross-check registrations, attach only that process's observation, and publish per-test freshness records only after source and runtime producer validation; keep standalone `verify` and `gate` cache serving available but forbid cached tests from satisfying command health; regression-test unbound failing tests, cached green witnesses beside newly red `TestMain` or examples, shadowed siblings, stale and unverifiable records, race-disabled policy invocations, and source/runtime drift, then close the mapped evidence gaps.
- [x] 8. Add the unified CLI `check` orchestration and protobuf result: compile the corpus, load records and policy, execute the policy once, verify bindings, evaluate coverage, surface failed witnesses, compute prune residue, and return one verdict without invoking `test`, `verify`, or `gate` as subprocesses; preserve deterministic JSON and quiet output, make human diagnostics projections of the report messages, retain the legacy operations as explicit diagnostics, and add command-level cancellation, exit-code, broken-binding, coverage-violation, and resolved-gap fixtures.
- [x] 9. Add MCP `check` and shared progress plumbing for `check`, `gate`, and expensive `context` slices: emit bounded phase and per-invocation progress with elapsed time, counts, and terminal deadline cause; keep progress off result payloads and CLI stdout; propagate client cancellation through package discovery and every child process; generate and validate MCP schemas against the protobuf result; extend cross-mode generators so CLI and MCP render equivalent facts; and close only the portions of the long-running-tools gap records proven by deadline and progress fixtures.
- [ ] 10. Migrate Stipulator's policy, Taskfile, and CI to unified execution: run `policy init`, review and commit its explicit policy, and replace the independent race-suite-plus-gate sequence only after an equivalence harness proves the unified check catches package build failures, `init`/`TestMain` failures, examples, fuzz seeds, no-test packages, all workspace members, binding failures, coverage violations, and prune residue; measure cold and warm execution on an isolated runner, report every omitted, executed, fresh, and uncacheable obligation, and close the implementation gaps only when the unified command is green.
- [ ] 11. Run the close-out gate: execute the isolated freshness diagnostic under the accepted policy and disposition its surfaced failure; promote lasting rationale into kept-current specs and comments; retarget every surviving issue citation; close every gap record this plan's landed work satisfies; execute and disposition the isolated freshness diagnostic per the reds-only-under-gate issue doc, closing it when dispositioned; retain or redefer the runtime-input and process-tree issues with self-contained conditions when their invariants remain open; verify all workspace modules, policy/report golden fixtures, CLI/MCP equivalence, cancellation, and the unified CI path; then delete this plan.
