# The blanket purity assertion widens what the spec sanctions as servable

Chunk 222 stated in REQ-evidence-witness-freshness that the accepted
policy's blanket assertion (`assume_pure`, one per invocation) suppresses
the unverifiable verdict for the invocation whole, beside the in-source
opt-in — a statement of what the code does (WithAssumePure; the `pure`
capture-group segment; this corpus's own invocations carry it). It is
soundness-sensitive: it widens the set of outcomes the spec sanctions
serving without re-running, made under the standing authority for the
user's audit.

Lands: user decision.
